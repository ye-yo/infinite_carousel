const defaultOptions = {
  startIndex: 0,
  slideToShow: 1,
  slideToScroll: 1,
  infinite: true,
  autoSlide: true,
  slideMargin: 10,
  previewMode: true,
  previewRatio: 0.5,
};

export default defaultOptions;
